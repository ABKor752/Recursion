#include <vector>

/**
 * Requires: Nothing
 * Modifies: Nothing
 * Effects:  Returns the power set of "set"
 */
std::vector<std::vector<int>> powerset(std::vector<int>& set) {
    // Solution
    return { {} };
}